---
- name: Create a new directory
  hosts: localhost
  become: yes
  tasks:
    - name: Create directory
      file:
        path: /home/ec2-user/aws
        state: directory
        mode: '0700'
        owner: ec2-user
        group: ec2-user 
        # xt5s44
